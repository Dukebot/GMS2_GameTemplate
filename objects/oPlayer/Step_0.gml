/// @description Insert description here
// You can write your code in this editor
getInput();

maxSpeed = 4;

if (right) x += maxSpeed;
if (left) x -= maxSpeed;
if (up) y -= maxSpeed;
if (down) y += maxSpeed;